import 'package:box_canvas/box_canvas.dart';

/// Function that is called when an entity is added
typedef AddEntityCallback = void Function(
    {required EntityPosition position, required String id});
